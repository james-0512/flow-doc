## 請求成功回應（回應攔截器的成功分支）

**觸發**：每一次 API 呼叫成功回應（HTTP 2xx）。

### 步驟

1. **原封不動回傳** `src/utils/service/api.service.ts:68`

成功路徑沒有任何全域處理。這一點值得知道：**回應的解包（`res.data.data`）、
成功提示、載入狀態解除都由各流程自己做**，不是攔截器代勞。所以流程敘述裡看到的
`loadingStore.removeLoadingKey()` 與成功提示，都是那條流程自己寫的。

### 資料變化

無。

### 異常與補償

不適用——這是成功分支。
