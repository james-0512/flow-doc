## 請求送出失敗（請求攔截器的錯誤分支）

**觸發**：請求在**送出之前**就失敗——例如請求攔截器本身拋出例外。

### 步驟

1. **原封不動往下拋** `src/utils/service/api.service.ts:51`
   不記錄、不顯示、不轉換。

這一分支刻意是空的。能走到這裡的只有「請求還沒離開瀏覽器就壞了」的情況，
在正常運作下不會發生；真正需要處理的網路與伺服器錯誤都在回應階段，
由「回應錯誤」那一節統一處理。

### 資料變化

無。

### 異常與補償

無補償行為。錯誤直接交給呼叫端的 `catch`。
