# web 服務：唯讀掛載共用 volume，root 指向 publish 換版的 current symlink。
# vitepress cleanUrls 產出的是無副檔名連結，try_files 補 .html。
server {
  listen 80;
  server_name _;
  root /site/current;
  index index.html;

  location / {
    try_files $uri $uri/ $uri.html =404;
  }
}
