# 已知限制

這份手冊由靜態分析產生。以下是**分析器看不到、或刻意不追**的東西。
讀手冊時請把這份清單當成「手冊沒說的部分」的完整列表——不在這裡的遺漏才是 bug。

這份檔案同時是靜態站「已知限制」頁的來源，內容改動會直接反映到產出。

## 一、刻意未實作（決策，非缺陷）

### SignalR 的 server push 未追蹤

專案有 63 處 SignalR 呼叫（`connection.value.on(eventName, callback)` /
`.invoke('connect', …)`）。**未接進流程圖。**

原因有二：

1. **事件名多半是變數而非字串常數**（`conn.on(eventName, cb)`），沒有可用的 join key。
   這與 plan.md §3 對動態事件名的判斷相同——強行猜測會產生假的流程步驟，
   而假步驟比缺步驟更傷手冊的可信度。
2. **語意上它不是既有鏈的延伸，而是新的流程起點。** SignalR 是後端主動推送，
   handler 被觸發時沒有使用者動作在前面。要正確表達它，需要一個新的 entry 類型
   （「系統推送觸發」），而不是在現有的 emit join 機制上硬接。

影響：涉及即時通知、看板即時更新的流程，手冊只會描述使用者主動觸發的那一半。

### BroadcastChannel 的跨分頁通訊未追蹤

專案有 6 處（代理模式的 token 同步與登出廣播）。數量少、且與 SignalR 同樣屬於
「非使用者觸發的流程起點」，一併延後。

影響：多分頁同時操作時的行為（例如一個分頁登出、其他分頁跟著登出）不在手冊內。

### reactive 連動（`watch` / `watchEffect`）未追蹤

專案有 239 處。`watch(() => store.x, …)` 沒有語法上的 join key，要接就得做
「誰寫了 `store.x`」的 data-flow 分析——成本高一個量級，且誤判會產生假步驟。

例外：`watch(…, { immediate: true })` 的第一次執行是確定的，語意上等同 `onMounted`，
但目前**也未納入**（見下方「二、覆蓋率缺口」）。

### `defineExpose` + template ref 呼叫未追蹤

專案有 43 處。父元件透過 `ref` 直接呼叫子元件方法，需要 template ref 的型別解析。

## 二、覆蓋率缺口（缺陷，待補）

### 頁面載入流程仍偏少

902 條流程中有 13 條由頁面載入觸發（判準放寬前只有 4 條）。仍然偏少的原因是
這個專案只有 60 個 SFC 使用 `onMounted`，其餘初始化散落在
`watch(…, { immediate: true })`（46 檔）與自訂 composable 中，目前都未被視為流程起點。

抽樣顯示那些 `immediate` watch 多半是 prop → 本地狀態同步，不是資料載入，
所以實際損失小於數字表面；但確實存在。

200 個路由指向的元件中，有 69 個完全沒有偵測到任何 entry——多為只渲染子元件的
版面容器。

影響：**手冊對「進入某頁時系統做了什麼」的覆蓋仍不完整。**

### 動態元件上的事件接不起來

`<component :is="currentTab.component" @some-event="handler">` 的標籤無法靜態解析成
檔案，因此子元件的 `emit` 找不到這個 listener。全 repo 有 121 處 emit 接不到，
其中最大宗即此類。

這與 plan.md §5 列的「interface 多型無法靜態決定實際走向」同類，屬靜態分析的固有極限。

### 點號 handler 未解析

`@click="modalInfo.okFn"` 這類把 handler 放在物件屬性裡的寫法，目前無法起鏈（97 處）。

### 賦值型副作用不列入副作用清單

分析器追的是**函式呼叫**，不追賦值。所以這些不會出現在副作用清單裡：

- axios 攔截器注入的標頭（`config.headers.Authorization = …`）
- 直接寫 store state（`store.x = value`）
- 元件本地狀態切換（`isShow = !isShow`）

**這不影響哪些流程進手冊。** 實測 984 條被排除的鏈中，598 條完全沒有副作用、
332 條只是取得 store handle、剩下 54 條呼叫的全是 UI 回饋（載入狀態、提示訊息、
modal 取消回呼），沒有一條是業務流程——也就是說即使補上賦值追蹤，流程分類一條都不會變。

它影響的是**已收錄流程的描述完整度**：副作用清單會低估。緩解方式是封包附上鏈上
每個函式的完整原始碼，敘述可以據此寫準（例如攔截器注入哪些標頭，讀原始碼就看得到）。

回訪條件：若出現「只改前端狀態、不打後端，但確實是業務動作」的需求（例如純前端
暫存的購物車），這條就要重新評估。

## 三、判準排除的東西

一條 entry 要成為「業務流程」，判準是**鏈中至少跨越一次 HTTP 邊界**（不論讀寫）。
流程再依此分成「寫入型」（會改變資料）與「查詢型」（只讀後端）。

被排除的是 987 條**完全沒碰後端**的鏈：開 modal、切 tab、toggle 顯示、
本地表單欄位互動。這些是互動細節而非業務流程。

若某個操作你認為是業務流程卻不在手冊裡，最可能的原因是它沒有任何後端互動——
例如純前端的計算或排序。

## 四、鏈的展開上限

- **深度上限 8 層**、**單鏈節點預算 250**。到達上限的節點在封包裡標為
  「達深度上限／達節點預算」，不會靜默截斷。
- **一個 emit 最多展開 5 個父層 listener**。共用元件可能被數十個父層使用，
  超出的數量會在該節點標示「另有 N 個父層也監聽此事件」。
- **同一個函式在同一條鏈中只完整展開一次**，後續出現以「同前」參照取代。
  這是為了避免封包被重複子樹灌爆（修正前最大的鏈有 76% 節點是重複的）。
